services:
  postgres:
    image: postgres:16
    container_name: distributed_math_postgres
    environment:
      POSTGRES_DB: distributed_math
      POSTGRES_USER: distributed_math
      POSTGRES_PASSWORD: distributed_math
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: